import { createCell } from '../modules/Cell.js';

export const CELL = { x: 0, 
                      y: 0,
                      alive: false,
                      neighbors: [],
                      next_state: false};
